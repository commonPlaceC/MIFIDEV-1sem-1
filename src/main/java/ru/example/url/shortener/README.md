# Сервис сокращения URL

Комплексный сервис сокращения URL, реализованный на Java с уникальными возможностями управления пользователями, отслеживания кликов и автоматического истечения срока действия.

## Возможности

### Основной функционал
- Преобразование длинных URL в короткий формат `clck.ru/xxxxxxx`
- Разные пользователи получают уникальные короткие коды для одного и того же URL
- Мониторинг и ограничение количества кликов по URL
- URL автоматически истекают через 24 часа
- Идентификация пользователей по UUID без аутентификации
- Автоматическое открытие браузера при доступе к коротким URL
- Переключение между пользователями без закрытия приложения

## Быстрый старт

### Запуск приложения

1. **Компиляция проекта:**
   ```bash
   cd PROJECT_DIR
   ./gradlew build
   ```

2. **Запуск основного приложения:**
   ```bash
   ./gradlew run -PmainClass=ru.example.url.shortener.UrlShortenerApplication
   ```

### Запуск тестов

1. **Запуск всех тестов:**
   ```bash
   ./gradlew test
   ```

2. **Запуск конкретного теста:**
   ```bash
   ./gradlew test --tests="ru.example.url.shortener.UrlShortenerServiceTest"
   ```

3. **Запуск тестов с подробным выводом:**
   ```bash
   ./gradlew test --info
   ```

4. **Запуск тестов с продолжением после ошибок:**
   ```bash
   ./gradlew test --continue
   ```

## Руководство по использованию

### Консольный интерфейс

Приложение предоставляет интерактивное консольное меню:

```
🔗 URL SHORTENER - MAIN MENU
============================================================
1. 📝 Create short URL          - Создать короткий URL
2. 📋 View my URLs              - Просмотреть мои URL
3. 🌐 Access short URL          - Перейти по короткому URL
4. 📊 View URL statistics       - Просмотреть статистику URL
5. 👤 User information          - Информация о пользователе
6. 🔧 System statistics         - Системная статистика
7. ⚙️ Manage my URLs            - Управление моими URL
8. 🧹 Clear my notifications    - Очистить мои уведомления
9. 🚪 Switch user (logout)      - Сменить пользователя (выйти)
10. ❌ Exit application         - Выйти из приложения
```

### Описание команд

#### 1. Создать короткий URL
- Позволяет ввести длинный URL для сокращения
- Создает уникальный короткий код для текущего пользователя
- Устанавливает лимит кликов (по умолчанию 100) и время истечения (24 часа)
- Показывает созданный короткий URL в формате `clck.ru/xxxxxxx`

#### 2. Просмотреть мои URL
- Отображает все URL, созданные текущим пользователем
- Показывает статус каждого URL (активный, истекший, заблокированный)
- Отображает количество использованных кликов и оставшееся время
- Показывает дату создания и истечения

#### 3. Перейти по короткому URL
- Позволяет ввести короткий код или полный короткий URL
- Проверяет валидность и доступность URL
- Увеличивает счетчик кликов
- Автоматически открывает оригинальный URL в браузере
- Показывает статистику использования

#### 4. Просмотреть статистику URL
- Позволяет ввести короткий код для получения подробной статистики
- Показывает информацию о кликах, времени создания и истечения
- Отображает текущий статус URL
- Показывает оригинальный URL

#### 5. Информация о пользователе
- Отображает UUID текущего пользователя
- Показывает дату создания пользователя
- Выводит количество созданных URL
- Показывает статистику активных и истекших URL

#### 6. Системная статистика
- Отображает общую статистику системы
- Показывает общее количество пользователей
- Выводит общее количество созданных URL
- Показывает количество активных URL в системе

#### 7. Управление моими URL
- Позволяет редактировать параметры созданных ссылок
- **Изменение лимита переходов**: Увеличение или уменьшение максимального количества кликов
- **Продление времени жизни**: Добавление дополнительных часов к сроку действия ссылки
- **Деактивация URL**: Принудительное отключение ссылки (необратимая операция)
- Все операции доступны только владельцу ссылки
- Изменения возможны только для активных ссылок

#### 8. Очистить мои уведомления
- Показывает количество pending уведомлений
- Позволяет очистить все уведомления пользователя
- Требует подтверждения перед очисткой

#### 9. Сменить пользователя (выйти)
- Позволяет выйти из текущей пользовательской сессии
- Возвращает к экрану выбора пользователя
- Не закрывает приложение полностью

#### 10. Выйти из приложения
- Полностью закрывает приложение
- Очищает все уведомления
- Корректно завершает все фоновые процессы

### Рабочий процесс пользователя

1. **Новые пользователи:**
   - Выберите "Create new user session"
   - Сохраните ваш UUID для будущих сессий

2. **Возвращающиеся пользователи:**
   - Выберите "Continue with existing user ID"
   - Введите ваш сохраненный UUID

3. **Создание коротких URL:**
   - Выберите опцию 1 из меню
   - Введите ваш длинный URL (например, `https://www.baeldung.com/java-9-http-client`)
   - Получите ваш уникальный короткий URL (например, `clck.ru/abc123`)

4. **Доступ к коротким URL:**
   - Выберите опцию 3 из меню
   - Введите короткий код или полный URL
   - Оригинальный URL автоматически откроется в вашем браузере

5. **Переключение пользователей:**
   - Выберите опцию 8 из меню
   - Подтвердите выход из текущей сессии
   - Консоль очистится, и вы сможете войти под другим пользователем

### Пример сессии

```
Введите URL для сокращения: https://www.baeldung.com/java-9-http-client

✅ УСПЕШНО!
📋 Оригинальный URL: https://www.baeldung.com/java-9-http-client
🔗 Короткий URL: clck.ru/aB3xY7z
🎯 Лимит кликов: 100
⏰ Истекает: 2024-11-09 14:30:00
```

## Технические характеристики

### Параметры по умолчанию
- **Лимит кликов**: 100 кликов на URL
- **Время истечения**: 24 часа с момента создания
- **Длина короткого кода**: 7 символов (кодировка Base62)
- **Интервал очистки**: Каждые 5 минут

### Поддерживаемые форматы URL
- `https://example.com`
- `http://example.com`
- `www.example.com` (автоматически добавляется https://)
- `example.com/path?query=value`
- `subdomain.example.com:8080/path`
- `localhost` и `localhost:port` (для разработки)

### Валидация URL
- Проверка корректности доменных имен
- Отклонение доменов, начинающихся или заканчивающихся дефисом
- Поддержка только положительных номеров портов
- Отклонение произвольных одиночных слов как доменов

## Команды Gradle

### Основные команды
```bash
# Компиляция проекта
./gradlew build

# Запуск приложения
./gradlew run -PmainClass=ru.example.url.shortener.UrlShortenerApplication

# Запуск тестов
./gradlew test

# Очистка проекта
./gradlew clean

# Создание JAR файла
./gradlew jar
```

### Команды для тестирования
```bash
# Запуск всех тестов
./gradlew test

# Запуск конкретного класса тестов
./gradlew test --tests="ru.example.url.shortener.UrlShortenerServiceTest"

# Запуск конкретного теста
./gradlew test --tests="ru.example.url.shortener.UrlShortenerServiceTest.testUserCreation"

# Запуск тестов с подробным выводом
./gradlew test --info

# Запуск тестов с продолжением после ошибок
./gradlew test --continue

# Просмотр отчета о тестах
# Отчет будет доступен в: build/reports/tests/test/index.html
```